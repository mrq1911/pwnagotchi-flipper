#pragma once
// Our repo's short commit, regenerated at build (apply_pwnfriend.py / the build script)
// and stamped on PWNFRIEND_ADV as fw=<hash>, so the Flipper's About screen shows exactly
// which firmware is flashed (the protocol number alone can't distinguish rebuilds).
// This committed value is a placeholder; the build overwrites it.
#define PWNFRIEND_FW_COMMIT "nogit"
